# 🛒 Product Catalog Service

Консольное приложение для управления каталогом товаров маркетплейса.  
Реализовано на **Java Core** с использованием коллекций, без сторонних зависимостей (Spring, Hibernate и т.д.).

## 📦 Функционал

- ✅ **CRUD-операции**: добавление, просмотр, редактирование, удаление товаров
- 🔍 **Гибкий поиск**: по категории, бренду, цене, названию (включая расширенную фильтрацию)
- 📁 **Сохранение данных** между запусками (в `products.json`)
- 📜 **Аудит действий** (в `audit.log`)
- 📊 **Метрики**: количество товаров в каталоге
- 🛡️ **Валидация данных**: запрет пустых полей, отрицательной цены, дубликатов названий

## 🏗️ Архитектура

- **Многослойная архитектура**: `model` → `repository` → `service` → `persistence` → `ui`
- **Паттерн Command** для обработки меню
- **Интерфейсы** для лёгкого переключения между реализациями (in-memory / file)
- **Потокобезопасное** хранилище (`ConcurrentHashMap`)

## ⚙️ Запуск

1. Соберите проект (Maven / Gradle) или запустите напрямую из IDE:
   ```bash
   javac -d out $(find src -name "*.java")
   java -cp out io.ylab.productcatalogservice.Main